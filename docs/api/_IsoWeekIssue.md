IsoWeekIssue
------------

ISO week issue type.

### Generics

*   `TInput` `extends string`

### Definition

*   `IsoWeekIssue` `extends BaseIssue<TInput>`
    *   `kind` `'validation'`
    *   `type` `'iso_week'`
    *   `expected` `null`
    *   `received` `` `"${string}"` ``
    *   `requirement` `RegExp`