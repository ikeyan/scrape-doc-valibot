declare module "disposablestack/DisposableStack" {
	type x = DisposableStack;
	const x: typeof DisposableStack;
	export = x;
}
declare module "disposablestack/AsyncDisposableStack" {
	type x = AsyncDisposableStack;
	const x: typeof AsyncDisposableStack;
	export = x;
}
